// import {
    
// } from '../actions';

const INITIAL_STATE = {
    isLoggedIn: false,

}

export default function userReducer(state = INITIAL_STATE, action) {
    switch (action.type) {


        default:
            return state;
    }
}